let nota = 6.5; // nota final
let pasoRecuperacion = true; // true si aprobó recuperación

if (nota >= 7 || pasoRecuperacion) {
  console.log("El estudiante aprueba.");
} else {
  console.log("El estudiante no aprueba.");
}
